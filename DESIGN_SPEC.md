# 光索科技 CT 影像工作站改版说明

版本：3.0 QML Medical Workstation Foundation
目标屏幕：1920 x 1080 工控屏，100% 缩放

## 1. 本阶段目标

本阶段将原 Qt Widgets 原型迁移为 QML 工作站框架，并接入最小可运行的医学影像链路。重点是先稳定页面架构、领域边界和视图容器，设备采集、重建和临床级算法将在后续阶段接入。

当前四页流程如下：

```text
患者确认 -> 联锁检查 -> 扫描范围 -> CT 影像工作站
   |            |            |             |
DICOM/身份    设备与安全    上下界/参考面   MPR/3D/分割/测量/导出
```

前三页各保留一个主操作；高频影像编辑集中到第四页，但通过左侧数据、中央视图和右侧上下文工具分区，避免形成单页参数墙。

## 2. 页面与主操作

| 页面 | 核心任务 | 主操作 | 关键安全反馈 |
|---|---|---|---|
| 01 患者确认 | 导入检查并核对双标识 | 确认患者并继续 | 未载入、标签缺失、读取失败 |
| 02 联锁检查 | 复核门禁、急停、探测器和运动状态 | 完成联锁检查 | 异常锁定、不可越级 |
| 03 扫描范围 | 设置上下界、方向和参考平面 | 确认范围并进入工作站 | 范围校验、截断风险 |
| 04 影像工作站 | 查看、分割、三维呈现、测量与导出 | 当前上下文中的唯一强调操作 | 数据来源、后端状态、错误信息 |

全局顶部持续显示品牌、患者/检查、流程状态、后端状态和数据载入状态。步骤条只允许进入已完成步骤或下一个合法步骤；锁定时停止前进。

## 3. 工作站布局

```text
┌ 全局状态：患者 / 检查 / 工作流 / 后端 ──────────────────────────┐
├ 01 患者确认 | 02 联锁检查 | 03 扫描范围 | 04 影像工作站 ────────┤
├ 全局影像工具：浏览 / 窗宽窗位 / 平移 / 缩放 / 测量 / 布局 ─────┤
│ 数据与派生对象 │ Axial                  │ 3D Volume │ 上下文工具 │
│ 患者/检查      ├────────────────────────┼───────────┤ 查看       │
│ Study/Series   │ Coronal                │ Sagittal  │ 分割       │
│ Segmentation  │                        │           │ 3D         │
│ Measurements  │                        │           │ 导出       │
└ 状态：序列 / 尺寸 / 间距 / 后端 ────────────────────────────────┘
```

固定侧栏宽度为 320 px 和 340 px，中央视图自适应剩余空间。视口标题和状态贴边显示，主图像保持最大面积。默认四视图可切换为仅三维或仅切片。

## 4. 信息与组件层级

| UI 区域 | QML 组件 | C++ 提供者 |
|---|---|---|
| 应用壳层与步骤条 | `qml/Main.qml` | `WorkflowController` |
| 患者确认 | `PatientPage.qml` | `MedicalDataController` |
| 联锁检查 | `SafetyPage.qml` | `WorkflowController`，后续设备网关 |
| 扫描范围 | `ScanRangePage.qml` | 当前 UI 状态，后续采集协议模型 |
| 左侧数据树 | `DataPanel.qml` | `MedicalDataController` |
| 四视图 | `ViewportPane.qml` | `MedicalViewportItem` |
| 右侧工具 | `InspectorPanel.qml` | DICOM、ITK、VTK 控制接口 |

QML 不持有 ITK/VTK 对象。C++ 将体数据和掩膜转换为只读快照，VTK 渲染线程从快照重建管线，避免 QML 直接跨线程访问医学数据。

## 5. 已接入能力

| 模块 | 当前实现 | 当前边界 |
|---|---|---|
| DICOM | 单文件、目录最大序列、患者/检查标签 | 尚无本地数据库、PACS、DICOMDIR 浏览器 |
| 二维显示 | 单幅 X 线、三个正交切片、窗宽窗位 | 尚无诊断级方向标记和十字线联动 |
| 基本工具 | 切片、平移/缩放、近似物理长度 | 测量尚未写入持久化对象，裁剪仅限 3D Z 轴 |
| 分割 | 阈值、Connected Threshold 种子生长 | 尚无画笔、撤销栈、形态学与统计 |
| 三维 | GPU 体绘制、MIP、裁剪、分割表面 | 尚无传递函数编辑器和多分割对象管理 |
| 导出 | 原始 DICOM 实例副本 | 尚无派生 DICOM、SEG、匿名化和 PACS 发送 |

## 6. 构建策略

MSVC 是完整后端的推荐和发布基线。仓库预设选择 v143 14.44，以兼容当前 VTK/ITK 包及 CUDA 12.8。CMake 不禁止其他 64 位 MSVC 工具集，但会提示它们尚未在当前依赖组合上验证。

MinGW 使用相同 QML 文件和控制器接口，当前关闭 `CT_ENABLE_MEDICAL_BACKEND`。这是 ABI 限制，不是 UI 分叉。未来提供 MinGW 编译的 VTK/ITK/RTK 后，可在该预设中打开完整后端。

当前 ITK 安装存在两个打包问题：`ITKVtkGlue` 带有旧机器的绝对 VTK 路径，`TotalVariation` 依赖缺失的 Eigen 配置。CMake 只加载当前功能实际需要的 GDCM、Thresholding 和 RegionGrowing 模块。RTK 尚未进入运行链路。

## 7. 视觉与交互规则

| Token | 值 | 用途 |
|---|---|---|
| `app` | `#111518` | 应用底色 |
| `panel` | `#1A1F23` | 两侧面板 |
| `control` | `#292F33` | 输入与普通按钮 |
| `image` | `#050708` | 医学影像背景 |
| `border` | `#343C41` | 分隔线和视口边界 |
| `text` | `#F0F2F3` | 标题和关键值 |
| `textSecondary` | `#8F9AA0` | 说明、坐标和次级值 |
| `accent` | `#DD8335` | 主操作、选中和警告重点 |
| `success` | `#62A178` | 完成和正常状态 |
| `danger` | `#C75850` | 锁定和错误 |

界面使用 8 pt 网格，紧凑控件高度不低于 40 px，关键确认按钮不低于 48 px。橙色不作为大面积背景，只表示当前活动工具、主操作、分割结果和需关注状态。

## 8. Qt 控件映射

| 交互 | QML/Qt 类型 |
|---|---|
| 应用窗口 | `ApplicationWindow` |
| 页面切换 | `Loader` + `WorkflowController` |
| 布局 | `ColumnLayout`、`RowLayout`、`GridLayout` |
| 模式选择 | `ButtonGroup`、`TabBar`、`ComboBox` |
| 参数 | `Slider`、`TextField`、`CheckBox` |
| 文件选择 | `FileDialog`、`FolderDialog` |
| 二维/三维视口 | `QQuickVTKItem` 子类 |
| 测量叠加 | QML `Canvas`，后续迁移到场景对象模型 |
| 异步数据状态 | C++ `Q_PROPERTY` + signals |

## 9. 空状态与错误状态

- 未选患者：视口显示导入入口，后续步骤不可进入；
- MinGW UI 模式：保留完整布局并明确显示“无医学后端”，不伪造影像；
- DICOM 无序列：保留当前数据并显示读取错误；
- 标签缺失：字段显示“未提供”，不得用演示值填充；
- 参数无效：拒绝分割并在左侧状态区显示具体原因；
- 导出不完整：报告已复制数/总数，不显示成功；
- 设备异常：状态机进入“异常/锁定”，禁止继续检查。

正式版本还需加入危险操作确认、审计日志、用户身份、协议签名、设备心跳和故障恢复步骤。

## 10. 后续排期

| 阶段 | 目标 | 主要交付 |
|---|---|---|
| Phase A 基础框架 | 已完成 | QML 四页、MSVC/MinGW 预设、DICOM/ITK/VTK 最小链路 |
| Phase B 影像交互 | 下一阶段 | 十字线联动、方向标记、活动视图、测量对象、2D ROI 裁剪、撤销重做 |
| Phase C 数据模型 | 待开始 | Patient/Study/Series 场景模型、本地数据库、多序列选择、派生对象持久化 |
| Phase D 高级分割 | 待开始 | 画笔、擦除、形态学、分割统计、表面平滑、DICOM SEG |
| Phase E 采集与重建 | 待依赖确认 | RTK/CUDA 接入、投影数据、几何标定、重建任务、GPU 资源监控 |
| Phase F 医疗安全 | 发布前必需 | 权限、审计、协议锁定、设备联锁、风险控制、验证与可追溯性 |

Phase B 开始前应先冻结坐标系约定（DICOM LPS、VTK 世界坐标、视图方向）和测量精度要求，否则后续十字线、分割和导出会产生重复修改。
