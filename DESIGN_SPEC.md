# 正交投影 CT 系统 UI 攵版说明

版本：1.0

目标屏幕：1920 x 1080 工控屏，100% 缩放

用户：放射技师、设备工程师

## 1. 设计目标

界面从“患者 / 采集 / Viewer 三个相互切换的功能页”改为由检查状态驱动的单一工作台。患者、任务和设备状态始终可见；中央区域只承载当前任务；右侧只显示当前任务需要的工具。橙色仅用于当前主操作、选择和警告，不作为大面积背景。

设计优先级：患者安全 > 状态可知 > 当前任务效率 > 功能可发现性 > 视觉表现。

## 2. 信息架构图

```text
正交投影 CT 操作控制台
├─ 全局状态栏（始终可见）
│  ├─ 产品 / 当前流程状态
│  ├─ 设备在线与自检
│  ├─ 机架联锁 / 门控 / 急停
│  ├─ PACS 网络
│  └─ 设备设置 / 工程入口
├─ 左侧患者与任务（始终可见）
│  ├─ 患者双标识
│  ├─ 检查申请与检查号
│  ├─ 已锁定协议
│  ├─ 六步任务进度
│  └─ 最近操作 / 完整审计日志
├─ 中央主工作区（状态唯一映射）
│  ├─ 患者确认：工作列表
│  ├─ 协议选择：匹配协议
│  ├─ 定位中：实时定位图像
│  ├─ 就绪：安全清单与曝光摘要
│  ├─ 采集中：扫描位置与采集进度
│  ├─ 查看结果：AP / LAT 主图像
│  └─ 异常锁定：错误与恢复步骤
└─ 右侧上下文工具（随状态切换）
   ├─ 身份核对
   ├─ 协议摘要 / 参数校验
   ├─ 参考平面 / 扫描上下界 / 朝向
   ├─ 设备联锁
   ├─ 实时采集状态
   ├─ 查看 / 测量 / 标注 / 导出
   └─ 异常处理
```

## 3. 页面布局草图

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 光索科技 │ 当前状态 / 下一步 │ 设备 │ 联锁 │ 急停 │ PACS │ 设置            │ 76
├──────────────┬─────────────────────────────────────────────┬─────────────────┤
│ 当前患者     │ 页面标题 / 当前任务说明                     │ 上下文工具      │
│ 双标识       │                                             │                 │
│              │                                             │ 当前状态所需    │
│ 检查任务     │              中央主工作区                   │ 的少量参数、    │
│ 协议         │                                             │ 校验或工具      │
│              │                                             │                 │
│ 任务进度     │                                             │                 │
│ 01 ... 06    │                                             │                 │
│              ├─────────────────────────────────────────────┤                 │
│ 最近操作     │ 审计提示                     [唯一主操作]    │                 │
└──────────────┴─────────────────────────────────────────────┴─────────────────┘
     304 px                  自适应，最小 808 px                   328 px
```

桌面端固定顶部 76 px、底部操作区 84 px、左右栏固定宽度。中心影像区优先扩展。1440 x 860 为原型最小尺寸；正式工控部署锁定 1920 x 1080，并关闭系统级缩放变化。

## 4. 状态机与主操作

| 状态 | 进入条件 | 中央内容 | 唯一主操作 | 离开条件 |
|---|---|---|---|---|
| 未选患者 | 系统启动 / 上一检查完成 | 今日工作列表 | 确认患者身份 | 双标识确认 |
| 已选患者 | 身份确认通过 | 协议匹配结果 | 暂不可用，提示先选协议 | 点击一个协议 |
| 已选协议 | 候选协议已选 | 协议匹配结果 | 锁定协议并进入定位 | 剂量、平面、设备范围校验通过 |
| 定位中 | 协议已锁定 | 定位相机与参考线 | 确认定位并执行校验 | 扫描范围、朝向、参考面通过 |
| 就绪 | 定位校验通过 | 安全清单与曝光摘要 | 开始曝光与采集 | 现场复核 + 危险操作二次确认 |
| 采集中 | 曝光确认 + 物理开关 | 实时扫描监控 | 不可用，仅显示采集中 | 采集完成或联锁中止 |
| 查看结果 | 图像重建完成 | AP / LAT Viewer | 完成检查并归档 | 图像可诊断、结果已进入发送队列 |
| 异常 / 锁定 | 任一关键联锁失败 | 错误码与恢复步骤 | 复核联锁并解锁 | 联锁恢复且设备复核通过 |

规则：状态转换由领域状态触发，不能由页面路由直接触发。采集中和锁定态禁止编辑患者、协议、曝光参数和定位参数。

## 5. 组件清单

| 组件 | 规格 | 用途 |
|---|---|---|
| GlobalStatusBar | 76 px 高，状态块最小 112 px | 产品、流程、设备和联锁总览 |
| PatientTaskRail | 304 px 宽 | 患者双标识、检查任务、流程、审计 |
| ContextRail | 328 px 宽 | 当前状态专属工具 |
| PageHeader | 24 px 标题 + 13 px说明 | 当前任务与操作预期 |
| PrimaryAction | 最小 270 x 52 px | 每个状态唯一橙色动作 |
| SecondaryButton | 最小高 44 px | 非关键动作或工具选择 |
| StatusChip | 低对比中性底 | 设备在线、联锁、网络状态 |
| SafetyCallout | 左侧 3 px 状态色 | 正常、警告、严重异常 |
| WorklistTable | 行高 58 px | 远距离扫描和低误触选择 |
| ProtocolCard | 三列平铺，不嵌套卡片 | 协议摘要、剂量、选择 |
| ScanCanvas | 黑色影像底 | 定位、采集、Viewer 主图像 |
| WorkflowStepper | 六步纵向 | 已完成、当前、未开始 |
| ViewerToolTabs | 查看 / 测量 / 标注 / 导出 | 防止工具条压过图像 |
| AuditSnippet | 时间 + 单条事件 | 最近关键操作反馈 |

按钮、输入框和可点击工具最小高度 40 px；关键按钮为 52 px。组件间距使用 8 pt 网格：8 / 16 / 24 / 32 px。圆角 3-5 px，避免消费级大圆角和浮动卡片感。

## 6. 关键交互与安全约束

1. 患者确认弹窗默认焦点为“否”，显示姓名、患者 ID、出生日期，要求双标识核对。
2. 协议选择不立即生效。点击候选项只进入“已选协议”；点击主操作并通过参数校验后才锁定。
3. 协议锁定后修改任何剂量、平面或扫描范围必须撤销就绪并重新校验。
4. 定位页默认只暴露参考平面、上下界和朝向；高级曝光参数进入受权限控制的二级面板。
5. 曝光前必须再次核对身份；主操作随后显示危险操作确认。弹窗默认焦点为取消。
6. 采集中不显示可编辑曝光参数，仅显示当前平面、机架位置、同步状态、进度和剂量。
7. 任何联锁错误立即停止高压、停止机架运动、冻结编辑并保留患者上下文。
8. 解锁不是“忽略错误”。只有设备状态服务返回联锁恢复后，主操作才可用。
9. 患者选择、身份确认、协议选择/锁定、参数变更、曝光确认、中止、解锁、发送和归档全部写审计日志。
10. Viewer 默认工具是查看；测量、标注和导出放入右侧分组，不占用主图像顶部空间。

## 7. 空状态和错误状态

| 场景 | 中央表现 | 主操作 / 恢复 |
|---|---|---|
| 工作列表为空 | 空表 + “当前筛选无待检患者” + 最近同步时间 | 次级“刷新工作列表”；允许受控新建患者 |
| 搜索无结果 | 保留搜索词，显示“未找到匹配患者” | 次级“清除筛选” |
| 患者信息不完整 | 缺失字段就地标红，禁止确认 | 返回 RIS 修正或工程权限下补录 |
| 无匹配协议 | 说明申请项目与设备能力不匹配 | 次级“查看全部协议”；要求审计原因 |
| 参数越界 | 参数行红色高亮并显示允许范围 | 禁止锁定，恢复推荐值后重检 |
| 定位画面丢失 | 图像区显示时间戳与相机错误码 | 禁止进入就绪；允许重连定位相机 |
| PACS 离线 | 顶部网络状态转警告，结果仍可查看 | 进入可靠本地发送队列，不阻断曝光 |
| 采集中断 | 锁定页显示已采集范围、剂量和原因 | 联锁复核后选择重采或结束，均需原因 |
| 图像重建失败 | 单幅图像错误占位，不清除原始数据 | 次级“重试重建”，失败后工程诊断 |
| 存储空间不足 | 顶部严重错误 + 锁定检查启动 | 清理或扩容后由设备服务解除 |

错误消息格式统一为：发生了什么 + 系统已经做了什么 + 操作者下一步 + 可检索错误码。禁止只显示“操作失败”。

## 8. 颜色建议

| Token | 建议值 | 用途 |
|---|---|---|
| `surface.app` | `#15191C` | 应用底色 |
| `surface.work` | `#111518` | 中央工作区 |
| `surface.rail` | `#1B2024` | 左右栏与操作栏 |
| `surface.control` | `#292F33` | 控件底色 |
| `border.default` | `#3A4247` | 分割和控件边界 |
| `text.primary` | `#F0F2F3` | 标题和关键值 |
| `text.secondary` | `#9DA7AC` | 说明文字 |
| `text.muted` | `#788389` | 审计与弱提示 |
| `accent.orange` | `#DD8335` | 主操作、选中、警告重点 |
| `status.ok` | `#668D76` | 正常状态，仅小面积使用 |
| `status.warning` | `#DF8739` | 警告边线和文本 |
| `status.danger` | `#C85F4E` | 严重错误，不用于普通删除 |
| `image.background` | `#080B0E` | 医学影像背景 |

文本与背景对比建议达到 WCAG AA。曝光中的 `X-RAY ON` 可使用橙底深字，但面积应控制在状态徽标范围；不要将整个页面变为橙色。

## 9. Qt 控件映射建议

| 设计组件 | Qt Widgets 映射 | 实现建议 |
|---|---|---|
| 应用壳层 | `QMainWindow` + `QHBoxLayout/QVBoxLayout` | 固定顶栏和左右栏，中心自适应 |
| 工作流页面 | `QStackedWidget` | 页面索引只由状态控制器设置 |
| 状态机 | `QStateMachine` / C++ 领域枚举 | 正式版建议 `QStateMachine` + guard 条件 |
| 工作列表 | `QTableView` + `QAbstractTableModel` | 正式版替换原型的 `QTableWidget`，支持 RIS 增量同步 |
| 协议选项 | `QButtonGroup` + checkable `QPushButton` | 单选只表示候选，锁定为独立命令 |
| 定位 / Viewer | 自定义 `QWidget::paintEvent` 或 OpenGL 视图 | 图像数据与 overlay 分层，标注使用场景对象 |
| Viewer 工具组 | `QTabWidget` | 四组固定顺序；保留各组最后工具 |
| 参数输入 | `QSpinBox/QDoubleSpinBox/QSlider` | 范围、单位、步进来自设备能力模型 |
| 安全清单 | `QCheckBox` | 关键核对不得预选，不持久化跨患者状态 |
| 危险确认 | `QMessageBox` 或自定义 `QDialog` | 取消为默认按钮，支持物理曝光开关状态 |
| 设备状态 | `QLabel` + 属性选择器 | 状态更新时重新 polish；避免颜色是唯一编码 |
| 操作日志 | `QListView` + 日志模型 | 写入时间、用户、角色、患者、设备状态、前后值 |
| 后台事件 | queued signal/slot | 设备线程不能直接操作 UI；按序列号拒绝过期事件 |

## 10. 前端开发改版说明

### 10.1 模块边界

- `WorkflowController`：唯一持有流程状态，校验合法转换，输出当前主操作。
- `PatientContext`：患者、检查申请、双标识核对结果；换患者时原子清空协议与图像上下文。
- `ProtocolController`：候选协议、选中协议、锁定快照、参数校验结果。
- `DeviceStateStore`：机架、探测器、高压、门控、急停、网络；提供聚合 readiness。
- `AcquisitionController`：曝光授权、进度、剂量、采集中止和原始数据状态。
- `ViewerController`：图像选择、窗宽窗位、测量、标注、发送队列。
- `AuditService`：不可由 UI 跳过的结构化操作日志。

### 10.2 必要数据契约

每个设备事件至少包含 `eventId`、`timestamp`、`deviceId`、`studyId`、`sequence`、`severity`、`code`、`message`。每次协议锁定保存完整参数快照与设备能力版本。曝光授权应同时绑定患者 ID、检查 ID、协议快照 ID 和定位校验 ID，避免界面状态串联错误。

### 10.3 状态一致性

- UI 不自行推断“就绪”；由设备状态、协议锁定和定位校验的聚合结果决定。
- 后台状态先落模型，再驱动视图。禁止按钮回调直接把页面跳到“采集中”。
- 联锁事件优先级高于其他页面事件。锁定期间到达的完成事件进入队列，待复核后处理。
- 换患者、重采和放弃检查需要事务式清理，不能保留上一患者的标注或协议。

### 10.4 验收要点

- 1920 x 1080 下无滚动即可完成患者到采集的主流程，主按钮从 2 m 观察仍可识别。
- 任一页面只有一个橙色主操作；Viewer 的工具选择不被误认为提交动作。
- 未完成身份复核、协议锁定、定位校验或联锁检查时无法开始曝光。
- 采集中所有影响曝光或身份的控件不可编辑。
- 门控断开、急停、探测器不同步、高压错误均可在一个 UI 帧内进入锁定反馈。
- 所有危险弹窗默认选择取消；键盘 Enter 不应意外触发曝光。
- 每个关键状态转换均有可追溯审计记录。
- 空列表、RIS/PACS 离线、参数越界、相机丢失、采集中断和重建失败均有明确恢复路径。

## 11. 原型范围

当前 Qt 原型实现工作流演示、状态反馈、模拟采集、Viewer 分组和门控联锁演示。影像、设备状态、剂量、RIS/PACS 与审计数据均为静态或模拟数据，不得直接用于临床。正式产品仍需完成 IEC 62304 软件生命周期、IEC 60601 相关可用性与风险控制、权限系统、持久化审计、设备通信和临床验证。
