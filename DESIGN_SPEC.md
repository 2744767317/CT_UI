# 光索科技正交投影 CT 影像工作站 UI 规范

版本：2.2 Four-Page Workflow

目标屏幕：1920 x 1080 工控屏，100% 缩放

## 1. 当前目标

当前版本采用四页顺序工作流，在控制界面复杂度的同时保留完整影像编辑能力，但不实现 DICOM 解析、MPR、体绘制、分割算法、设备通信或重建算法。所有影像、体数据、标签和状态均为界面占位数据。

四页分别承担患者身份、设备安全、扫描范围和影像编辑，不把不同认知任务堆叠到同一个页面，也不拆成过多细碎步骤。

## 2. 页面架构

```text
┌ 全局患者、检查、设备、联锁与 PACS 状态 ─────────────────────────┐
├ 01 患者确认 ─ 02 联锁检查 ─ 03 扫描范围 ─ 04 影像编辑 ─────────┤
├──────────────────────────────────────────────────────────────────┤
│ Page 1  工作列表 + 患者双标识核对                               │
│ Page 2  六项设备联锁 + 现场曝光条件                             │
│ Page 3  AP/LAT 定位画面 + 上下界 + 朝向 + 参考平面              │
│ Page 4  数据树 + Axial/Coronal/Sagittal/3D + 上下文属性         │
└──────────────────────────────────────────────────────────────────┘
```

前三页使用宽阔的单任务布局，只有第四页使用左侧 330 px、右侧 350 px 和中央自适应影像区。影像页两侧通过 `QSplitter` 调整。

## 3. 四页检查流程

| 页面 | 核心任务 | 唯一主操作 | 补充能力 |
|---|---|---|---|
| 01 患者确认 | 选择工作列表并完成双标识核对 | 确认患者并继续 | 搜索、申请项目和检查号 |
| 02 联锁检查 | 复核设备联锁和机房曝光条件 | 完成联锁检查 | 参数范围、预计剂量、返回复核 |
| 03 扫描范围 | 设置 AP/LAT 上下界、朝向和参考平面 | 确认范围并进入影像编辑 | 推荐范围恢复、截断风险反馈 |
| 04 影像编辑 | 查看、重建、分割、测量和导出 | 按当前模块提供明确动作 | DICOM、PACS、布局、异常日志 |

顶部步骤条始终显示当前位置。只能进入已完成步骤或下一个合法步骤，返回复核不会清除已完成数据；修改关键条件后，正式状态机应撤销相关下游校验。

## 4. 四视图工作区

默认布局为：

| 位置 | 视图 | 标识色 | 主要信息 |
|---|---|---|---|
| 左上 | Axial 轴状位 | 红色 | S 坐标、切片序号、窗宽窗位 |
| 左下 | Coronal 冠状位 | 绿色 | A 坐标、切片序号、窗宽窗位 |
| 右下 | Sagittal 矢状位 | 黄色 | R 坐标、切片序号、窗宽窗位 |
| 右上 | 3D Volume | 蓝灰色 | 体绘制预设、GPU 帧率 |

三个切片视图包含十字线、方向标记、切片滑杆和坐标信息。3D 视图包含方向盒、裁剪框、相机和体绘制状态。支持双击单视图最大化、2 x 2、1 + 3、单 3D、AP/LAT 双投影布局和活动视图互斥选中。

## 5. 编辑页数据栏

第四页左侧不再重复前三页内容，只显示当前患者摘要，以及 Patient、Study、Series、Projection、Volume、Segmentation 和 Measurement 场景树。

数据树是未来统一医学影像场景的 UI 入口，当前支持节点显示隐藏、选择、重命名和上下文操作。真实 DICOM 层级和本地数据库在后续成像引擎阶段接入。

## 6. 右侧上下文属性

| 标签页 | 当前 UI 能力 | 引擎接入后能力 |
|---|---|---|
| 显示 | 节点、窗宽窗位、体绘制预设、不透明度 | 传递函数编辑、裁剪平面 |
| 重建 | 输入输出、方案、重建核、体素与范围 | 重建任务、进度、质量比较 |
| 分割 | 活动分割、画笔、阈值、区域生长、平滑 | 撤销栈、分割预览、表面生成 |
| 测量 | 长度、角度、Cobb、ROI、体积 | 视图交互、统计与结构化结果 |
| DICOM | 对象层级、关键标签、匿名化和发送入口 | 数据库、PACS、标签浏览器 |

工具面板只显示当前节点相关属性。正式实现不得把所有参数同时铺开。

## 7. 全局工具栏

全局工具只保留跨视图高频操作：导入、PACS、选择、十字线、窗宽窗位、平移、缩放、测量、撤销、重做、布局和适窗。

分割、重建和 DICOM 详细命令归属右侧模块。各视图自身只显示标题、坐标、最大化和切片位置，不设置压过图像的大工具栏。

## 8. 视觉规范

| Token | 值 | 用途 |
|---|---|---|
| `surface.app` | `#111518` | 应用底色 |
| `surface.panel` | `#1A1F23` | 两侧面板 |
| `surface.control` | `#292F33` | 输入与按钮 |
| `surface.image` | `#050708` | 医学影像背景 |
| `border.default` | `#343C41` | 分隔和视口边界 |
| `text.primary` | `#F0F2F3` | 标题、患者和关键值 |
| `text.secondary` | `#8F9AA0` | 说明与坐标 |
| `accent.orange` | `#DD8335` | 主操作、活动工具和警告 |
| `view.axial` | `#C75850` | Axial 标识与联动线 |
| `view.coronal` | `#62A178` | Coronal 标识与联动线 |
| `view.sagittal` | `#C5A549` | Sagittal 标识与联动线 |
| `view.3d` | `#6C8FB0` | 3D 视图标识 |

四个视图标识色属于医学视图坐标编码，不与流程警告色混用。橙色仍只用于主操作、活动工具和警告重点。

## 9. 代码模块

```text
mainwindow.*              应用壳层和区域装配
ui/appstyle.*             全局 QSS 与视觉 token
ui/workflowpages.*        患者确认、联锁检查和扫描范围页面
ui/acquisitionpanel.*     编辑页当前患者摘要和数据树
ui/dicomdialog.*          本地导入、PACS 查询和传输队列
ui/viewportgrid.*         四视图布局
ui/imagingviewport.*      单个切片或 3D 视图占位组件
ui/inspectorpanel.*       显示、重建、分割、测量和 DICOM 属性
ui/safetylockdialog.*     联锁异常、恢复步骤和事件日志
tests/ui_smoke.cpp        离屏 UI 布局和模块回归测试
tests/ui_capture.cpp      四页视觉基线截图
```

UI 组件编译为 `eos_ui_components` 静态库，主程序和冒烟测试复用同一套组件。接入渲染引擎时，用 VTK 视图替换 `ImagingViewport` 的绘制区域，外部布局和面板接口保持不变。

## 10. 已完成的 UI 阶段

| 阶段 | 状态 | 交付内容 |
|---|---|---|
| Phase 1 工作站框架 | 完成 | 四页工作流、四视图、场景树、属性模块、代码拆分 |
| Phase 2 视图交互 | 完成 | 互斥焦点、双击最大化、四种布局、工具模式、快捷键、面板显隐 |
| Phase 3 数据工作流 | 完成 | DICOM 导入、PACS 查询、传输队列、重建进度、匿名化和导出入口 |
| Phase 4 编辑与安全 | 完成 | 分割工具、测量结果、3D 裁剪、采集进度、曝光确认、联锁锁定和事件日志 |

下一阶段不再扩充静态 UI，而是建立 `core/`、`models/`、`dicom/`、`imaging/` 和 `rendering/`，逐步用真实领域状态和医学成像引擎替换占位数据。

## 11. 当前限制

当前自绘影像只用于评估布局，不表示实际 CT、MPR 或 3D 重建能力。真实体数据必须来自有效的 DICOM CT 序列或设备重建输出；仅有 AP/LAT 投影时不得在界面中伪装成真实断层体数据。
